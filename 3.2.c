#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n;

    scanf("%d", &n);

    int a = 1, b = 1;

    for (i = 0; i < n - 2; i++)
    {
        if (i == 0)
    }
    return 0;
}
